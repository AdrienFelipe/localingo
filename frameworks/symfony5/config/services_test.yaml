services:
    _defaults:
        public: true

    # Keep service available in tests as it is not directly used otherwise.
    App\Shared\Domain\Repository\RepositoryInterface:
        class: App\Localingo\Infrastructure\Repository\Redis\RedisRepository
        arguments:
          [ '@Predis\Client' ]
